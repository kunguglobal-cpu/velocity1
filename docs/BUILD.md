# Build

GitHub Actions builds `app-debug.apk` with JDK 17 and Gradle 9.7.

The current app implements the onboarding shell. MetaApi credentials are entered by the user at runtime and are not committed to source control.
